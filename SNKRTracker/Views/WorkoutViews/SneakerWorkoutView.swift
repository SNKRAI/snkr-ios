struct SneakerWorkoutView: View {
    var runs: [RunningWorkoutContainer]

    var body: some View {
        VStack(alignment: .leading) {
            Section(header: HeaderView(text: "Past workouts").padding(.horizontal, Layout.headerPadding)) {
                Text("ferferfers")
            }
        }
    }
}
